<!DOCTYPE html>
<html>
<head>
  <title><%= full_title(yield(:title)) %></title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

	<div id="wrapper">
		<header>
			<h1><%= link_to "Members Only", root_path, id: "logo" %></h1>
			<ul>
				<% if logged_in? %>
					<li><%= link_to "Log out", logout_path, method: :delete %></li>
				<% else %>
					<li><%= link_to "Sign in", login_path %></li>
				<% end %>
			</ul>
		</header>
		<div id="content">
			<% flash.each do |message_type, message| %>
				<div class = "alert <%= message_type %>">
					<%= message %>
				</div>
			<% end %>
			<%= yield %>
		</div>
		<footer>
			<ul>
				<li class="center">Very basic gossip blog built by <a href="http://katemcfaul.com">katemcfaul.com</a></li>
				<li class="center">~~~</li>
				<li>Concept: A visitor to the site gets to see the gossip, but not who posted it. Log in and the names show up.</li>
			</ul>
		</footer>
	</div>
		
	  <%= debug(params) if Rails.env.development? %>
</body>
</html>
